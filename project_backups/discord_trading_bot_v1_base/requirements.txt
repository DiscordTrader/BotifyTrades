discord.py-self>=1.9.0
webull>=0.2.0
requests>=2.28.0
python-dotenv>=1.0.0

# Windows-only: Required for secure credential storage (DPAPI)
pywin32>=306; platform_system=="Windows"

# Optional: Only needed for building Windows EXE
# pyinstaller>=6.0.0
